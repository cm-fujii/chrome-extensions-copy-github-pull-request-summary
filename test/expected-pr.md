# Apply `font-variation-settings` to the suggestion widget

{ORIGIN}/microsoft/vscode/pull/200000

`chengluyu:fix/font-variation-settings` → `main`

## Description

## 背景

詳細は [RFC](https://example.com/rfc) を参照。`SIGTERM` の扱いを変更する。

- [x] Google プロバイダ対応
- [ ] GitHub プロバイダ
  - スコープ設定は**後続PR**

### 設定例

```ts
const token = await refresh({
  retry: 3,
})
```

```release-note
None
```

| 環境 | 値 |
|---|---|
| dev | `false` |
| prod | `true` |

> 注意: 移行後は旧 API を削除する。

2. 二番目
3. 三番目

---

**詳細な手順**

折りたたみの中身。

[#199954]({ORIGIN}/microsoft/vscode/issues/199954) と [括弧つき URL](<https://example.com/a_(b)>)、~~削除済み~~。
